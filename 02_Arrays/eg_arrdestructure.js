let demo = ["Gaurav", "Krunal", "Yash"]; 

let [E1, E2] = demo;  // Destructuring first two elements from `demo`
let EName = [E1, E2]; // Creating a new array with extracted values

console.log(EName);
