/* let cars = ["bmw", "toyato", "marcedeez" ];
console.log(cars.length); 
console.log(cars[cars.length-2]);  */

let age = [10,20,40,"gaurav"];
for(let i=0; i<age.length; i++){
    // console.log(age[i]);
    console.log(age[i].toUpperCase());  // not print while using the string mehtod the array has to be all string 
}

