const computer = ["samsung", "sony", "lenonvo"];
const newComputer=[];

for (let brands of computer) {
    console.log(brands);
    // newComputer.push(brands.toUpperCase());  // apply to the value not to the whole array like computer
} 
// console.log(newComputer);
