// let userStatus = "logged_in";
// let currentStatus = userStatus == "logged_in" ? "user is logged in" : "user is logged out";

// function loggedCheckInStatus(status) {
//     return status ? "user_logged" : "user_logOut";
// }
// console.log(loggedStatus(1)); // Output: "user_logged"

// function loggedCheckInStatus(status) {
//     return status ? "User is logged in" : "User is logged out";
// }
// console.log(loggedCheckInStatus(0)); 

// best way = 
const user_Logged = true;
const userStatus =  user_Logged ? "class-1" : "class-2"
 console.log(userStatus);

